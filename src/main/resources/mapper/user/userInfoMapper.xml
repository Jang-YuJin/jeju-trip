<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trip.jeju.user.mapper.UserInfoMapper">

    <select id="selectUserById" resultType="com.trip.jeju.user.vo.UserVO">
        SELECT
              EMAIL
            , NICKNAME
            , NAME
            , IMG
            , AUTH_PROVIDER
            , CASE WHEN ROLE = 'DELETE' THEN TRUE ELSE FALSE END AS IS_DELETED
        FROM USER WHERE id = #{id}
    </select>

    <update id="withdrawUser">
        UPDATE USER
        SET ROLE = 'DELETE'
        WHERE ID = #{id}
    </update>
</mapper>